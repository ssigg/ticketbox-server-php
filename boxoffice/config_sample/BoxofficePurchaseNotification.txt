Boxoffice {$boxoffice} sold some tickets. The seats listed below are now purchased.

Purchased Seats
===============
{foreach $reservations as $reservation}
{$reservation->event->name}, {$reservation->event->dateandtime} - {$reservation->seat->name}: {$reservation->price}  $ {if $reservation->isReduced}(reduced){/if}
{/foreach}

Total: {$total} $